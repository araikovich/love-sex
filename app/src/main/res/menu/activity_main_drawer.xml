<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_position_menu"
            android:title="@string/sex_positions_title" />
        <item
            android:id="@+id/nav_gallery"
            android:icon="@drawable/ic_cat_menu"
            android:title="@string/dirty_scenes_title" />
        <item
            android:id="@+id/nav_slideshow"
            android:icon="@drawable/ic_fire_menu"
            android:title="@string/sex_missions_title" />
    </group>
</menu>